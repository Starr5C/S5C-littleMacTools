# 本机验收记录

验收日期：2026-08-18

## 内建视网膜显示器

- 显示器逻辑区域：1470 × 956；
- 可用区域：1470 × 868；
- 左侧预留比例：4.42%，当前分辨率下为 65 点；
- 目标窗口：位置 (65, 33)，尺寸 1405 × 868；
- Finder 实际窗口：位置 (65, 33)，尺寸 1405 × 867；
- 最大误差：1 点，通过 2 点容差；
- 连续运行两次，位置与尺寸无漂移。

## 外接显示器 27M2U

- 显示器逻辑区域：2560 × 1440；
- 可用区域：2560 × 1410；
- 左侧预留比例：3.125%，当前分辨率下为 80 点；
- 目标窗口：位置 (-479, -1410)，尺寸 2480 × 1410；
- Finder 实际窗口与目标完全一致；
- 连续运行两次，位置与尺寸无漂移。

两块显示器均通过 `AXFocusedWindow` 选择当前焦点窗口；Finder 同时在两屏各有窗口时，能调整被激活的那一个。Dock 位于底部且不自动隐藏，两个窗口底边均与各自 `visibleFrame` 底边一致，没有被 Dock 遮挡。

比例伸缩测试同样通过：内建屏可用宽度模拟为 1920 时预留自动变为 85 点；外接屏可用宽度模拟为 3840 时预留自动变为 120 点。源码不再使用固定预留点数。
